# Kokoro-82M TTS Requirements
# ============================
# Dependências para executar o script kokoro_tts.py

# Biblioteca Kokoro (PyTorch) - baixa modelos automaticamente do HuggingFace
kokoro>=0.9.2

# SoundFile para salvar arquivos de áudio WAV
soundfile>=0.12.0

# PyTorch (backend para Kokoro)
torch>=2.0.0

# NumPy para manipulação de arrays de áudio
numpy>=1.24.0
